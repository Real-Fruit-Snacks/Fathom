# users

> Display a list of logged in users.
> See also: `who`.
> More information: <https://www.gnu.org/software/coreutils/users>.

- Display currently logged in usernames:

`users`

- Display logged in usernames from a specific file (defaults to `/var/run/utmp`):

`users </var/log/wmtp>`